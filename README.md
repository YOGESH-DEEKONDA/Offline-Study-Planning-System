# Offline-Study-Planning-System
A lightweight C++ offline study planning system. Utilizes efficient scheduling algorithms to generate optimized, personalized timetables based on subject priority, workload constraints, and user deadlines without requiring internet connectivity.
